<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- ScrollView to wrap the ListView -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <!-- LinearLayout to contain the ListView -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#FFFFFF">

            <!-- ListView with white background and black text -->
            <ListView
                android:id="@+id/list_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="30dp"
                android:background="#FFFFFF"
                android:textColor="#000000" />
        </LinearLayout>
    </ScrollView>

    <!-- RelativeLayout for buttons at the bottom -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="#FFFFFF">

        <!-- BACK Button -->
        <Button
            android:id="@+id/back_button"
            android:layout_width="80dp"
            android:layout_height="wrap_content"
            android:text="BACK"
            android:textColor="#EBE2E2"
            android:background="@drawable/back_button_rounderd"
            android:textSize="20dp"
            android:layout_alignParentStart="true"
            android:layout_marginEnd="20dp"/>

        <!-- Start Quiz Button -->
        <Button
            android:id="@+id/buttonStartQuiz"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#EDEBEB"
            android:textStyle="bold"
            android:background="@drawable/back_button_rounderd"
            android:text="Start Quiz"
            android:layout_alignParentEnd="true"/>
    </RelativeLayout>

</LinearLayout>
